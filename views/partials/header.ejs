<header>
  <h1><a href="/">Club99</a></h1>
  <nav>
    <nav>
      <% if(!currentUser){ %>
      <a class="login" href="/log-in">Log In</a>
      <a href="/sign-up">Sign Up</a>
      <% } else { %>
      <% if(!currentUser.is_member){ %>
      <a href="/membership" class="become-member-btn">Become Member</a>
      <% } %>
      <a href="/log-out">Log Out</a>
      <% } %>
    </nav>
  </nav>
</header>
